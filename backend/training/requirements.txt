# ── Data pipeline ─────────────────────────────────────────────────────────────
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.0
geopy>=2.4          # Nominatim geocoding fallback
pandas>=2.2
tqdm>=4.67

# ── Training ──────────────────────────────────────────────────────────────────
torch>=2.3
transformers>=4.44
datasets>=2.21
peft>=0.12          # LoRA / QLoRA
trl>=0.10           # SFTTrainer
bitsandbytes>=0.43  # 4-bit quantisation (QLoRA)
accelerate>=0.33    # multi-GPU + device_map
scipy>=1.13
wandb>=0.17         # optional experiment tracking

# ── Inference server ──────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27

# ── Shared with main backend (already in backend/requirements.txt) ─────────────
# numpy, pydantic, python-dotenv — included here so training/ is self-contained
numpy>=2.0
pydantic>=2.8
python-dotenv>=1.2
