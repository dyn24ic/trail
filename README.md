Duplicate the .env.frontend.example and .env.backend.example and seed the keys.

To run: simply
```bash
docker compose up -d
```

To rebuild then run:
```bash
docker compose up -d --build
```
(with nextjs, django, scala for specific)